import { AppPage } from './app.po';
import { element } from 'protractor';

describe('weather-app App', () => {
  let page: AppPage;

  beforeEach(() => {
    page = new AppPage();
  });

  //it('should display header level 2', () => {
  //  let title = element(headertwo).getTitle;
  //  expect(page.getParagraphText()).toEqual('Welcome to app!');
  //});
});
